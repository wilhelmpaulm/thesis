<?php

class Complaint_addressesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('complaint_addresses')->truncate();

		$complaint_addresses = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('complaint_addresses')->insert($complaint_addresses);
	}

}
